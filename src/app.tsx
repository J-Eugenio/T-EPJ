import React from 'react';
import { SafeAreaView } from 'react-native';

import Home from './Pages/Home';

const Main: React.FC = () => {
  return (
    <SafeAreaView>
      <Home />
    </SafeAreaView>
  );
};

export default Main;
